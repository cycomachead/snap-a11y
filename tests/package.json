{
  "name": "snap-a11y-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Accessibility test harness for Snap! (roles, labels, focus, accessibility tree)",
  "type": "commonjs",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.62.1"
  }
}
